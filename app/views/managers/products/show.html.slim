.container
  .row.title-field
    .col
    .col-auto
      .card
        .large_image
          = image_tag product.avatar.url(:large)
        .name_of_product
          h3 = link_to product.name,[:managers, product]
        .description_of_product
          = product.description
        .stock
          = product.current_amount
        .price
          = product.price_in_dollars
        - if manager_signed_in?
          = link_to 'Edit', edit_managers_product_path(product), class: 'btn btn-outline-info product_button'
          = link_to 'Destroy',[:managers, product], method: :delete, data: {confirm: 'Are you sure?'}, class: 'btn btn-outline-danger product_button'
        = link_to 'Back', root_path, class: 'btn btn-outline-secondary product_button'
    .col
